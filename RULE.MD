Never assume.

If something cannot be confirmed from:

- current code
- current JSON
- existing components

then stop and explain the uncertainty instead of creating a new implementation.

Never silently redesign architecture.

Never silently change the JSON schema.

Every schema change must be justified.

Every new component must solve a reusable problem.

Every rendering change must remain backward compatible.