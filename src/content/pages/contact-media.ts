/**
 * Contact page imagery — sourced from the live site's existing asset paths
 * (same pattern as `home-media.ts`). Swap these two URLs if final assets
 * differ; nothing else in the Contact page hardcodes an image path.
 */
export const contactMedia = {
  /** Photo beside the "Contact Information" list. */
  infoPhoto: '/themes/default/assets/images/contact-3-img_os4jyj.jpg',

  /** Background behind the dark "Get In Touch With Us!" form card. */
  formBackground: '',
} as const
